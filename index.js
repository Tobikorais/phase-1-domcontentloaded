document.addEventListener('DOMContentLoaded', () => {
    const targetElement = document.getElementById('text'); // Assuming the element has an ID of 'text'
    targetElement.textContent = 'This is really cool!';
});